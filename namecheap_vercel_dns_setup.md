# Настройка DNS для restodocks.com (Namecheap → Vercel)

## Текущая ситуация
- ✅ Домен куплен на Namecheap: restodocks.com
- ✅ Namecheap DNS панель открыта: https://ap.www.namecheap.com/Domains/DomainControlPanel/restodocks.com/advancedns
- 🔄 Нужно: добавить домен в Vercel и настроить DNS записи

---

## ЧАСТЬ 1: Добавление домена в Vercel (получение DNS записей)

### Шаг 1: Найти проект в Vercel
1. Откройте https://vercel.com/dashboard
2. Найдите ваш проект (вероятно называется "restodocks" или "restodocks-flutter")
3. Кликните на проект

### Шаг 2: Перейти в настройки доменов
1. В проекте кликните на вкладку **Settings** (в верхнем меню)
2. В левом меню выберите **Domains**

### Шаг 3: Добавить домен
1. Нажмите кнопку **Add** или **Add Domain**
2. Введите: `restodocks.com`
3. Нажмите **Add**

### Шаг 4: Выбрать environment
Vercel спросит, к какому environment привязать домен:
- Выберите **Production** (рекомендуется для основного домена)
- Git Branch: **main** (или ваша основная ветка)
- Нажмите **Add**

### Шаг 5: Получить DNS записи
После добавления Vercel покажет статус домена. Скорее всего будет:
- ❌ **Invalid Configuration** или **DNS Not Configured**

Vercel покажет **необходимые DNS записи**. Обычно это:

**Для apex домена (restodocks.com):**
```
Type: A
Name: @
Value: 76.76.21.21
```

⚠️ **ВАЖНО:** Точный IP адрес Vercel может отличаться! Используйте тот, что показывает Vercel.

**Альтернативный вариант (CNAME для apex):**
Некоторые проекты Vercel используют:
```
Type: CNAME
Name: @
Value: cname.vercel-dns.com
```

**Для www субдомена (опционально):**
Если хотите добавить www.restodocks.com:
```
Type: CNAME
Name: www
Value: cname.vercel-dns.com
```

### Шаг 6: Скопировать DNS записи
Скопируйте DNS записи, которые показывает Vercel (они будут видны в интерфейсе).

---

## ЧАСТЬ 2: Настройка DNS в Namecheap

Вы уже находитесь в правильном месте: **Advanced DNS** панель

### Шаг 1: Очистить старые записи (если есть)
В разделе **Host Records** найдите и удалите:
- Старые A записи для `@` (если есть)
- Старые CNAME записи для `@` или `www` (если конфликтуют)

**Как удалить:**
- Нажмите на иконку корзины (🗑️) справа от записи

### Шаг 2: Добавить A запись для apex домена

1. Нажмите **Add New Record**
2. Заполните поля:
   ```
   Type: A Record
   Host: @
   Value: [IP адрес из Vercel, обычно 76.76.21.21]
   TTL: Automatic (или 3600)
   ```
3. Нажмите ✓ (галочка) для сохранения

### Шаг 3: Добавить CNAME для www (рекомендуется)

1. Нажмите **Add New Record** еще раз
2. Заполните поля:
   ```
   Type: CNAME Record
   Host: www
   Value: cname.vercel-dns.com
   TTL: Automatic (или 3600)
   ```
3. Нажмите ✓ (галочка) для сохранения

### Шаг 4: Сохранить изменения
- Namecheap обычно сохраняет автоматически после каждой записи
- Убедитесь, что записи отображаются в списке **Host Records**

### Пример правильной конфигурации:

```
┌────────┬──────┬──────────────────────────┬─────────┐
│ Type   │ Host │ Value                    │ TTL     │
├────────┼──────┼──────────────────────────┼─────────┤
│ A      │ @    │ 76.76.21.21             │ Auto    │
│ CNAME  │ www  │ cname.vercel-dns.com    │ Auto    │
└────────┴──────┴──────────────────────────┴─────────┘
```

---

## ЧАСТЬ 3: Проверка и ожидание

### Шаг 1: Подождать распространение DNS
- DNS изменения могут занять от **5 минут до 48 часов**
- Обычно для Namecheap это **15-30 минут**

### Шаг 2: Проверить DNS в терминале

```bash
# Проверить A запись
dig restodocks.com +short
# Должно показать IP Vercel (например, 76.76.21.21)

# Проверить CNAME для www
dig www.restodocks.com +short
# Должно показать cname.vercel-dns.com или IP Vercel

# Проверить через внешний DNS
nslookup restodocks.com 8.8.8.8
```

### Шаг 3: Вернуться в Vercel
1. Откройте Vercel Dashboard → Ваш проект → Settings → Domains
2. Найдите `restodocks.com` в списке
3. Нажмите **Refresh** или подождите автоматической проверки
4. Статус должен измениться:
   - ❌ Invalid Configuration → ✅ Valid

### Шаг 4: SSL Сертификат
После того как DNS настроен:
- Vercel автоматически выпустит SSL сертификат
- Это занимает **5-15 минут**
- Статус изменится на **✅ Valid** с зеленой галочкой

---

## ЧАСТЬ 4: Добавление www субдомена в Vercel (опционально)

Если вы добавили CNAME для www в Namecheap, теперь добавьте www в Vercel:

### Вариант A: Редирект www → restodocks.com (рекомендуется)
1. В Vercel: Settings → Domains → **Add Domain**
2. Введите: `www.restodocks.com`
3. Выберите: **Redirect to restodocks.com**
4. Нажмите **Add**

### Вариант B: Отдельный www субдомен
1. Добавьте `www.restodocks.com` как отдельный домен
2. Выберите Production environment
3. Vercel будет показывать контент на обоих доменах

---

## Проверка после настройки

### Тест 1: DNS записи
```bash
dig restodocks.com +short
# Ожидаемый результат: 76.76.21.21 (или другой IP Vercel)

dig www.restodocks.com +short
# Ожидаемый результат: cname.vercel-dns.com или IP Vercel
```

### Тест 2: HTTP/HTTPS
```bash
curl -I https://restodocks.com
# Ожидаемый результат: HTTP/2 200 или 301/302 редирект
```

### Тест 3: В браузере
- Откройте https://restodocks.com
- Должен загрузиться ваш Flutter сайт
- SSL сертификат должен быть валидным (замок в адресной строке)

---

## Troubleshooting (Решение проблем)

### Проблема 1: "Invalid Configuration" в Vercel не исчезает
**Причина:** DNS еще не распространился или неправильные записи  
**Решение:**
1. Проверьте DNS через `dig restodocks.com +short`
2. Если показывает старый IP (216.198.79.1) - подождите еще
3. Если показывает IP Vercel - нажмите Refresh в Vercel

### Проблема 2: "DNS Not Found"
**Причина:** Запись еще не распространилась  
**Решение:** Подождите 30-60 минут, затем проверьте снова

### Проблема 3: "Domain already in use"
**Причина:** Домен используется в другом Vercel проекте  
**Решение:**
1. Найдите другой проект с этим доменом
2. Удалите домен оттуда
3. Добавьте в текущий проект

### Проблема 4: SSL сертификат не выпускается
**Причина:** DNS не настроен правильно или не прошло достаточно времени  
**Решение:**
1. Убедитесь, что DNS записи правильные
2. Подождите 15-30 минут
3. Проверьте, что домен имеет статус Valid в Vercel

---

## Альтернативный метод: Vercel Nameservers (более простой)

Если хотите, чтобы Vercel полностью управлял DNS:

### В Namecheap:
1. Domain List → Manage → Domain → Nameservers
2. Выберите **Custom DNS**
3. Введите:
   ```
   ns1.vercel-dns.com
   ns2.vercel-dns.com
   ```
4. Сохраните

⚠️ **Внимание:** 
- Все DNS записи (включая email MX записи) будут удалены
- Настройте их заново в Vercel после смены nameservers
- Распространение: до 48 часов

---

## Следующие шаги

1. ✅ Откройте Vercel Dashboard
2. ✅ Добавьте домен `restodocks.com` в ваш проект
3. ✅ Скопируйте DNS записи из Vercel
4. ✅ Вернитесь в Namecheap Advanced DNS
5. ✅ Добавьте A запись с IP от Vercel
6. ✅ Добавьте CNAME для www (опционально)
7. ⏳ Подождите 15-30 минут
8. ✅ Проверьте в Vercel, что домен стал Valid
9. ✅ Откройте https://restodocks.com в браузере

**Готово!** 🎉
