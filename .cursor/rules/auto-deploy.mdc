---
description: Автоматический деплой после изменений в коде
alwaysApply: true
---

# Автодеплой

После завершения любых изменений в коде (редактирование, добавление, удаление файлов) **обязательно** выполни коммит и push, чтобы изменения автоматически попали в деплой на Cloudflare Pages.

## Действия после правок

1. Выполни в корне репозитория:
   ```bash
   git add -A && git status
   ```
2. Если есть изменения — закоммить и запушь:
   ```bash
   git commit -m "Update: $(date +%Y-%m-%d\ %H:%M)" && git push
   ```
3. Деплой запустится автоматически при push в `main` или `staging` (Cloudflare Pages).

## Важно

- Не пропускай этот шаг — без push изменения не попадут на сайт.
- Если пользователь на другой ветке — уточни, пушить ли в main или в текущую ветку.
