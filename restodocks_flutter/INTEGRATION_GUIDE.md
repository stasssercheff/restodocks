# 🚀 Руководство по интеграции Supabase

## ✅ Что уже сделано:

### 1. **База данных настроена**
- ✅ Все таблицы созданы в Supabase
- ✅ Добавлены базовые технологические процессы
- ✅ Настроены связи между таблицами

### 2. **Flutter приложение обновлено**
- ✅ Supabase инициализирован в `main.dart`
- ✅ Созданы сервисы для работы с Supabase:
  - `AccountManagerSupabase` - управление аккаунтами
  - `ProductStoreSupabase` - управление продуктами
  - `TechCardServiceSupabase` - управление ТТК
- ✅ Обновлены модели для работы с Supabase (snake_case)
- ✅ Обновлены экраны для использования новых сервисов

## 📋 Следующие шаги:

### Шаг 1: Генерация JSON сериализации
Выполните в терминале:
```bash
cd restodocks_flutter
flutter pub run build_runner build --delete-conflicting-outputs
```

Это создаст файлы `.g.dart` для всех моделей.

### Шаг 2: Тестирование подключения
1. Запустите приложение:
   ```bash
   flutter run -d chrome
   ```

2. Перейдите в **Профиль → Тест Supabase**
3. Проверьте статус подключения

### Шаг 3: Тестирование регистрации
1. Зарегистрируйте новую компанию
2. Проверьте, что данные сохранились в Supabase
3. Войдите в систему

### Шаг 4: Загрузка продуктов
После регистрации можно загрузить продукты в Supabase:
- Используйте файлы из `assets/products/`
- Или добавьте продукты через интерфейс приложения

## 🔧 Настройка RLS (Row Level Security)

Если нужно настроить политики безопасности вручную, используйте файл `supabase_policies.sql`.

## 📊 Проверка данных в Supabase

1. Откройте [Supabase Dashboard](https://supabase.com/dashboard)
2. Перейдите в **Table Editor**
3. Проверьте созданные таблицы:
   - `establishments`
   - `employees`
   - `products`
   - `cooking_processes`
   - `tech_cards`
   - `tt_ingredients`

## 🎯 Готово к использованию!

Приложение теперь полностью интегрировано с Supabase и готово к работе! 🎉