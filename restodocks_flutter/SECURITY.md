# 🔐 Защита Restodocks Web Application

## Обзор защиты

Приложение включает несколько уровней защиты от копирования и несанкционированного использования:

### 1. Domain Validation (Валидация домена)

**Файл:** `lib/services/domain_validation_service.dart`

- Проверяет, что приложение запускается только на разрешенных доменах
- Блокирует работу на подозрительных доменах
- Логирует попытки доступа с неподдерживаемых доменов

**Разрешенные домены:**
- `restodocks.vercel.app` - Vercel deployment
- `restodocks.com` - Основной домен (добавить при получении)
- `www.restodocks.com` - WWW версия
- `localhost:*` - Для разработки

**Добавление нового домена:**
```dart
static const List<String> _allowedDomains = [
  'your-new-domain.com',
  'www.your-new-domain.com',
  // ... существующие домены
];
```

### 2. Code Obfuscation (Обфускация кода)

**Настройки:** `vercel-build.sh`

- Автоматическая обфускация при `--release` сборке
- Переименование переменных, функций и классов
- Удаление комментариев и отладочной информации

**Включение:**
```bash
flutter build web --release --dart-define=FLUTTER_WEB_OBFUSCATE=true
```

### 3. Server-side Protection (Серверная защита)

- **API Rate Limiting** - ограничение количества запросов
- **CORS политика** - запрет кросс-доменных запросов
- **JWT токены** - аутентификация с коротким сроком жизни
- **IP whitelisting** - разрешенные IP адреса

### 4. Monitoring (Мониторинг)

- **Логирование** подозрительных доступов в консоль браузера
- **Domain alerts** при попытке запуска на запрещенных доменах
- **API usage analytics** для отслеживания необычной активности

## Как работает защита

### При запуске:
1. Проверяется текущий домен
2. Если домен не в списке разрешенных:
   - Показывается предупреждение пользователю
   - Логируется информация для анализа
   - Приложение не запускается

### В production:
- Код обфусцирован и минифицирован
- Переменные и функции имеют случайные имена
- Удалена отладочная информация

## Обновление доменов

При развертывании на новый домен:

1. **Обновите список в `domain_validation_service.dart`:**
   ```dart
   static const List<String> _allowedDomains = [
     'new-domain.com',
     // ... другие домены
   ];
   ```

2. **Пересоберите приложение:**
   ```bash
   flutter build web --release --dart-define=FLUTTER_WEB_OBFUSCATE=true
   ```

3. **Разверните на Vercel**

## Ограничения защиты

### Что можно скопировать:
- ✅ Весь frontend код (HTML/CSS/JS)
- ✅ Дизайн и пользовательский интерфейс
- ✅ Клиентская бизнес-логика

### Что НЕЛЬЗЯ скопировать:
- ❌ Серверная логика (Supabase Edge Functions)
- ❌ База данных и пользовательские данные
- ❌ API ключи и секреты
- ❌ Серверная бизнес-логика

## Рекомендации

1. **Регулярно обновляйте список доменов**
2. **Мониторьте логи на подозрительную активность**
3. **Используйте HTTPS везде**
4. **Регулярно обновляйте зависимости**
5. **Настройте мониторинг API использования**

## Контакты

При обнаружении подозрительной активности или необходимости добавить новый домен, обратитесь к разработчикам.