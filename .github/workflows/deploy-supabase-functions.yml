name: Deploy Supabase Edge Functions

on:
  push:
    branches: [main, master]
    paths:
      - 'restodocks_flutter/supabase/functions/**'
      - 'supabase/functions/**'
      - '.github/workflows/deploy-supabase-functions.yml'

env:
  SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}

jobs:
  deploy-production:
    name: Deploy to Production
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Check SUPABASE_ACCESS_TOKEN
        run: |
          if [ -z "${{ secrets.SUPABASE_ACCESS_TOKEN }}" ]; then
            echo "::error::SUPABASE_ACCESS_TOKEN is not set. Add it in GitHub Settings → Secrets."
            exit 1
          fi
          echo "Token is set (length OK)"

      - name: Setup Supabase CLI
        uses: supabase/setup-cli@v1
        with:
          version: latest

      - name: Deploy Edge Functions (Production)
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
        run: supabase functions deploy --project-ref osglfptwbuqqmqunttha
        working-directory: restodocks_flutter

  deploy-staging:
    name: Deploy to Staging (Demo)
    runs-on: ubuntu-latest
    continue-on-error: true  # не падать, если Staging в другом аккаунте
    env:
      SUPABASE_ACCESS_TOKEN: ${{ secrets.STAGING_SUPABASE_ACCESS_TOKEN || secrets.SUPABASE_ACCESS_TOKEN }}
    steps:
      - uses: actions/checkout@v4

      - name: Setup Supabase CLI
        uses: supabase/setup-cli@v1
        with:
          version: latest

      - name: Deploy Edge Functions (Staging)
        run: supabase functions deploy --project-ref kzhaezanjttvnqkgpxnh
        working-directory: restodocks_flutter
