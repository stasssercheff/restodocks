# Грандплан продукта Restodocks

Единая точка правды: **что уже в продукте**, **что в бэклоге**, **какие внешние документы уточняют эпики**. Обновляйте этот файл при закрытии крупных фич или смене приоритетов.

**Статусы:** `[x]` сделано в текущей кодовой базе · `[ ]` не сделано · `[~]` частично / заготовка

---

## 1. POS / зал / касса виртуальная

| Задача | Статус | Где в коде / БД |
|--------|--------|------------------|
| Столы, этаж/зал, статусы | [x] | `pos_dining_tables`, экраны столов |
| Заказы зала, строки, оплата, split, чаевые | [x] | `pos_orders`, `pos_order_lines`, `pos_order_payments` |
| Касса зала: смены, выдачи, отчёты | [x] | `pos_cash_shifts`, `pos_cash_disbursements`, `PosCashHallService` |
| KDS, отчёт смены, закрытые счета | [x] | `pos_*` экраны, `router` |
| Настройки отображения заказов | [x] | `PosOrdersDisplaySettingsScreen` |

---

## 2. Склад (упрощённая модель, граммы)

| Задача | Статус | Где в коде / БД |
|--------|--------|------------------|
| Остатки и движения по заведению | [x] | `establishment_stock_balances`, `establishment_stock_movements` |
| Списание по ТТК при закрытии счёта | [x] | `PosStockService`, RPC `apply_establishment_stock_delta` |
| Защита от двойного списания по строке | [x] | `pos_order_line_id`, уникальный индекс для `pos_sale` |
| Приход по факту из списков заказов | [x] | `import`, `order_list_detail_screen` + `order_list_units` |
| Ручная корректировка (инвентаризация) | [x] | `adjustment`, FAB на `PosStockScreen` |
| Сводка месяца: приход / расход / корр. | [x] | `aggregateStockReconciliation`, `pos_stock_screen` |
| Сверка остатка vs суммы движений | [x] | `warehouse_health_check` |
| **Партии, сроки годности, несколько складов, штрихкоды** | [ ] | Отдельная модель БД и UI (полноценный WMS) |
| **Полная инвентаризация с расхождениями по актам** | [ ] | Зависит от WMS / бух. требований |

---

## 3. ТТК и согласование изменений

| Задача | Статус | Где в коде / БД |
|--------|--------|------------------|
| Заявки на изменение ТТК не-владельцем | [x] | `tech_card_change_requests`, инбокс |
| Согласование владельцем | [x] | `TechCardChangeRequestService`, экран инбокса |

---

## 4. Закупки (списки поставщикам)

| Задача | Статус | Где в коде / БД |
|--------|--------|------------------|
| Списки заказов, отправка, сохранённые | [x] | `order_lists`, Edge `save-order-document` |
| Факт «получено» и приход на склад | [x] | `receivedQuantity`, см. п. 2 |
| **Единый финансовый контур «закупка–продажа–деньги»** | [ ] | ERP-уровень, отчёты P&L по заведению |

---

## 5. Наблюдаемость и ошибки

| Задача | Статус | Где в коде / БД |
|--------|--------|------------------|
| Таблица `system_errors`, RLS | [x] | миграции POS/stock |
| Журнал в настройках (owner / ГД) | [x] | `SystemErrorsScreen` |
| Edge `log-system-error` | [x] | `supabase/functions/log-system-error` |
| Fallback записи через Edge при сбое insert | [x] | `SystemErrorService` |

---

## 6. Фискализация и ККТ

| Задача | Статус | Где в коде / БД |
|--------|--------|------------------|
| Пресеты налогов, настройки заведения | [x] | `establishment_fiscal_settings`, пресеты JSON |
| Очередь фискализации (outbox) | [~] | `fiscal_outbox`, сервисы |
| **Интеграция с физической ККТ / драйвер** | [ ] | Внешнее оборудование, отдельный контур |

---

## 7. Импорт номенклатуры и ИИ

| Задача | Статус | Подробности |
|--------|--------|-------------|
| Единый экран модерации импорта | [x] | `ImportReviewScreen`, маршрут `/import-review`, модель `ModerationItem`, буфер `ImportReviewPayload`; поток вставки текста / AI-парсинга и **интеллектуальный Excel** → один экран перед записью в БД |
| Edge Functions `ai-parse-product-list` и др. по ТЗ | [~] | Парсинг списка уже вызывается из клиента; отдельные функции из ТЗ — по необходимости |
| Поиск дубликатов / снятие лишних с номенклатуры | [x] | Номенклатура: локально (точн.+fuzzy `normalizeProductAliasKey`) + опционально Edge `ai-find-duplicates` (≤150 позиций), диалог `_DuplicatesDialog` |
| Слияние карточек продуктов в одну сущность в БД | [ ] | Сейчас снимается с номенклатуры; полное merge с переносом ссылок ТТК — отдельная задача |

---

## 8. Внешние системы (iiko и др.)

| Задача | Статус | Подробности |
|--------|--------|-------------|
| Подготовка к iiko | [~] | [IIKO_INTEGRATION_PREP.md](IIKO_INTEGRATION_PREP.md) |

---

## 9. Инфраструктура и релизы

| Задача | Статус | Подробности |
|--------|--------|-------------|
| Beta = `staging`, Prod = `main` | [x] | [DEPLOY_RULES.md](DEPLOY_RULES.md) |
| SQL-бандл POS/stock/cash | [x] | `restodocks_flutter/supabase/MANUAL_MIGRATION_POS_CASH_STOCK.sql` |
| Админка (Workers) | [~] | Ручной деплой по правилам репозитория |

---

## Бэклог крупных эпиков (что ещё не закрыто)

Ориентир для следующих итераций; детали — в таблицах разделов выше.

| Приоритет | Эпик | Раздел |
|-----------|------|--------|
| 1 | Слияние карточек продуктов в БД (перенос ссылок ТТК), не только снятие с номенклатуры | §7 |
| 2 | Довести `fiscal_outbox`, затем физическая ККТ | §6 |
| 3 | WMS (партии, сроки, склады, штрихкоды) → актовая инвентаризация | §2 |
| 4 | ERP: P&L и единый контур «закупка–продажа–деньги» | §4 |
| 5 | iiko и другие внешние системы | §8 |

---

## Как вести этот документ

1. Закрыли эпик — поменяйте `[ ]` на `[x]`, добавьте путь к файлам или миграции.
2. Новый крупный эпик — новый раздел или подпункт с явной формулировкой «вне текущего релиза».
3. Разделы 2–4 «до конца» в смысле **ERP/WMS** намеренно оставлены открытыми: это отдельные проекты по срокам и ТЗ.

Последнее обновление структуры плана: **2026-03-28**.
